apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Labels applied to all resources and pod template selectors.
# Overlays extend this with an environment-specific label.
commonLabels:
  app: webapp
  managed-by: kustomize

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
