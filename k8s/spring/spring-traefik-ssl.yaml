apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: spring-traefik-ssl
  annotations:
    kubernetes.io/ingress.class: traefik
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - spring.tsadimas.dns-cloud.net
      # Cert-manager will create this secret automatically containing the cert
      secretName: spring-prod-tls
  rules:
  - host: "spring.tsadimas.dns-cloud.net"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: spring
            port:
              name: spring